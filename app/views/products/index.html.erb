<p id="notice"><%= notice %></p>

<h1>Listing Products</h1>

<% if signed_in? %>
  <%= link_to 'New Product', new_product_path, class: "btn btn-primary" %>
<% end %>

<% @products.each do |product| %>
<div class="row thumbnail">
  <div class="col-md-4"><%= image_tag(product.image_url) %></div>
  <div class="col-md-8 caption">
    <h2><%= product.title %> <span class="badge"><%= product.price %> р.</span></h2>
    <p><%= product.description %></p>
    <p>
    <%= link_to 'Подробнее', product, class: "btn btn-default" %>
    <% if signed_in? %>
    <%= link_to 'Редактировать', edit_product_path(product), class: "btn btn-success" %></td>
    <%= link_to 'Удалить', product, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
    <% end %>
  </p>
  </div>
</div>

<% end %>
