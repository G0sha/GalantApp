<% provide(:title, @product.title) %>

<ol class="breadcrumb">
  <li><%= link_to "Каталог", products_path %></li>
  <li><%= link_to @groups.find(@product.group).name, "#" %></li>
  <li><%= link_to @subgroups.find(@product.subgroup).name, "#" %></li>
  <li class="active">Данные</li>
</ol>

<div class="row thumbnail margin-clear">
    <h2 class="text-center"><%= @product.title %> <span class="badge"><%= @product.price %> руб.</span></h2>
    <div class="col-xs-4 col-xs-offset-4"><%= image_tag(@product.image_url, class: "img-responsive") %></div>
    <div class="col-xs-12"><%= @product.description  %></div>
    <div class="col-xs-12">
      <%= link_to "Назад", "#", onclick: "history.back()", class: "btn btn-warning" %>
      <% if signed_in? %>
        <%= link_to 'Редактировать', edit_product_path(@product), class: "btn btn-success" %></td>
        <%= link_to 'Удалить', products_path, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
        <% end %>
  </div>
</div>

<ol class="breadcrumb">
  <li><%= link_to "Каталог" %></li>
  <li><%= link_to @groups.find(@product.group).name, "#" %></li>
  <li><%= link_to @subgroups.find(@product.subgroup).name, "#" %></li>
  <li class="active">Данные</li>
</ol>
